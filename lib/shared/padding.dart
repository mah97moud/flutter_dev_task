import 'package:flutter/material.dart';

const edgeInsetsH10 = EdgeInsets.symmetric(horizontal: 10);
const edgeInsetsOnlyR15 = EdgeInsets.only(right: 15);
const edgeInsetsAll5 = EdgeInsets.all(5);
const edgeInsetsAll10 = EdgeInsets.all(10);
const edgeInsetsAll20 = EdgeInsets.all(20);
